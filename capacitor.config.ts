import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hybrid-programing-unit-2',
  webDir: 'www'
};

export default config;
